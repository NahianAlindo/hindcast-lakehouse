# Copy this file to .env (already git-ignored) and fill in real values.
# .env is a Phase-0 stopgap only — these move into Azure Key Vault once
# infra/terraform provisions it in Phase 1 (see docs/PLAN.md §2.2, §Phase 1).

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# Filled in later, after scripts/bootstrap_oauth.py is written (Phase 2):
SPOTIFY_REFRESH_TOKEN=
